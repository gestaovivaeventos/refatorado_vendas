/**
 * Página de Indicadores Secundários
 * Redireciona para a página principal mantendo a rota
 */

import Dashboard from './index';

export default function IndicadoresPage() {
  return <Dashboard />;
}
